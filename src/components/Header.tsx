import React from "react";
import "../styles/Header.css"; 
import '../styles/colors.css';

const Header: React.FC = () => {
  return (
    <header className="zentria-header">
    </header>
  );
};

export default Header;
